<form method="post" action="{{ path('app_air_craft_category_delete', {'id': air_craft_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ air_craft_category.id) }}">
    <button class="btn">Delete</button>
</form>
