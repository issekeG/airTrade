<form method="post" action="{{ path('app_new_letter_delete', {'id': new_letter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ new_letter.id) }}">
    <button class="btn">Delete</button>
</form>
