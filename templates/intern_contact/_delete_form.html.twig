<form method="post" action="{{ path('app_intern_contact_delete', {'id': intern_contact.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ intern_contact.id) }}">
    <button class="btn">Delete</button>
</form>
